// Stub for react-dom — not available in React Native.
// Packages like @clerk/clerk-react import react-dom but only use it for web-specific
// code paths that never execute on native. Exporting an empty object is enough.
module.exports = {};
